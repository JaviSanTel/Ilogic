export { GoteoPuzzle } from '../../types/puzzle';

/** Estado: índice de letra seleccionada por columna (o undefined). Las columnas de espacio se omiten. */
export type GoteoState = (number | undefined)[];
